/**
 * Placeholder type for the Supabase database schema.
 *
 * In later steps, when we create actual database tables and run Supabase
 * code generation, this file will be auto-generated with real types for
 * every table, view, and function in the database.
 *
 * For now, it's just an empty placeholder so the Supabase client can be
 * typed generically (e.g. SupabaseClient<Database>) without errors.
 */
export interface Database {
  public: Record<string, never>;
}
